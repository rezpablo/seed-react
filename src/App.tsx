import * as React from 'react';
import Router from './Router';
import Menu from './main/template/Menu';

const App = () => (
    <div>
        <Menu/>
        <Router/>
    </div>
);

export default App;